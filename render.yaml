services:
  - type: web
    name: anomaly-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    # deploys are triggered by GitHub Actions (deploy hook) after tests pass
    autoDeploy: false
